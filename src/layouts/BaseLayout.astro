---
import '@fontsource/syne/700.css';
import '@fontsource/inter/400.css';
import '@fontsource/inter/600.css';
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import CookieConsent from '../components/CookieConsent.astro';
import { company } from '../data/site';

interface Props {
  title: string;
  description?: string;
  /** true = Header liegt transparent über dem Hero (nur Startseite) */
  transparentHeader?: boolean;
}

const {
  title,
  description = `${company.name} – Brücken- und Bauwerksinstandsetzung für öffentliche und gewerbliche Auftraggeber in ganz NRW.`,
  transparentHeader = false,
} = Astro.props;
---

<!doctype html>
<html lang="de" class="no-js">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="theme-color" content="#333333" />
    <script is:inline>
      // Kennzeichnet früh, dass JS verfügbar ist (steuert Reveal-Fallback in CSS)
      document.documentElement.classList.remove('no-js');
    </script>
  </head>
  <body>
    <a class="skip-link" href="#main">Zum Inhalt springen</a>
    <Header transparent={transparentHeader} />
    <main id="main">
      <slot />
    </main>
    <Footer />
    <CookieConsent />
    <script>
      import '../scripts/animations';
    </script>
  </body>
</html>

<style>
  .skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 200;
    background: var(--color-dark);
    color: #fff;
    padding: 0.6rem 1rem;
  }

  .skip-link:focus {
    top: 0;
  }
</style>
